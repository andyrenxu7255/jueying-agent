# 架构优化建议

1. **统一 LLM 调用层**: 消除 3 份 callLiteLLM → LlmClient
2. **统一 DB 连接池**: 消除 6 份 new Pool() → PoolManager
3. **统一渠道适配**: Feishu/Wecom 接口抽象 → ChannelAdapter
4. **统一服务启动**: 消除 ~30 行/服务的 bootstrap 重复 → createMicroService
5. **统一意图分类**: 规则 + LLM 两层架构 → IntentClassifier