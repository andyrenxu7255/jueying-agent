# 性能优化建议

1. **TTLMap**: 替换 hermes-adapter memoryStore 和 mobile-app notificationStore 的 Map
2. **Intent 规则分类**: 减少 ~30% 的 LLM 调用（高频场景规则先行）
3. **连接池复用**: PoolManager 统一管理，减少重复建立连接
4. **微服务启动**: createMicroService 减少各服务初始化开销